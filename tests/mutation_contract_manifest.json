{
  "slice": "gx-mutation-contract-slice-3",
  "measured_diff_chars": 15999,
  "note": "HOUSE RULE 4, AMENDMENT REQUESTED, A SLICE 4 IS NEEDED. Slice 3 was declared the last. Its 13 items measured 24609 diff chars of content ALONE against a binding 16000 that may not be raised, and the fallback of landing the content and leaving the flip alone does not fit either, so slice 3 landed the execution engine and the outcome reader only: the oracle's third conjunct is unmet, and so its second. Enforcement stays INERT, nothing is half-armed, no gate is written, the AWAITING pin has not risen.",
  "remaining": [
    {"item": "occurrence-index", "file": "tests/mutation_contract.py",
     "obligation": "where no slice is unique even so, the entry ALSO carries an explicit occurrence index and the check asserts THAT occurrence -- the LAST RESORT after the multi-line widening slice 1 landed."},
    {"item": "shape-gate", "file": "tests/test_gcp_mutation_machinery.py",
     "depends_on": ["two-states-floor-and-pin", "register"],
     "obligation": "THE SHAPE GATE over WHATEVER entries the register holds -- green with zero entries -- per entry, in BOTH states, the state COMPUTED from the tree rather than declared by an author."},
    {"item": "Removal", "file": "tests/mutation_contract.py",
     "obligation": "Removal CARRIES id, subject, family keyed to FAMILY_KINDS, apply(monkeypatch), and must_fail; applied IN PROCESS by monkeypatch, NEVER by renaming files: offline, reversible, no debris."},
    {"item": "removal-fixture", "file": "tests/conftest.py", "depends_on": ["Removal", "register"],
     "obligation": "A session autouse fixture reads GCP_TEST_REMOVAL=<id> and applies it, AFTER collection, which makes the probe-before-removal ordering hold."},
    {"item": "removal_failure", "file": "tests/mutation_contract.py", "depends_on": ["Removal"],
     "obligation": "a deliberately inert Removal whose apply does nothing must make the checker FAIL."},
    {"item": "enforcement-arms", "file": "tests/test_gcp_mutation_machinery.py",
     "depends_on": ["shape-gate", "owner-presence-table", "node-id-collection"],
     "obligation": "each must make the checker FAIL on a bad entry and stay green over zero entries: every element a node id that COLLECTS; every named test module inside the OWNER's own declared paths; owner a gx- task id this document declares; the signals exhibited by flipping one presence slice in a scratch copy; the floor FIRING on an entry whose owner is present; the AWAITING debt PRINTED with id, owner, unresolved field and source."},
    {"item": "machinery-self-tests-remaining", "file": "tests/test_gcp_mutation_machinery.py",
     "depends_on": ["removal_failure"],
     "obligation": "the inert Removal, its Mutation twin whose after equals its before failing BOTH the anchor and must-fail checks, and the outcome reader on all four shapes, the skip arm naming the ordering inversion."},
    {"item": "arm-enforcement", "file": "tests/test_gcp_mutation_machinery.py",
     "depends_on": ["enforcement-arms", "machinery-self-tests-remaining"],
     "obligation": "THE CONTRACT'S ENFORCEMENT STAYS INERT UNTIL THE FINAL SLICE FLIPS IT LIVE IN ONE SMALL MEASURED STEP; a half-armed gate is the single outcome no slice may produce."}
  ]
}
