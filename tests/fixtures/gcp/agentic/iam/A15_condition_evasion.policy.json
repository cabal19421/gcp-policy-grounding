{
  "bindings": [
    {
      "condition": {
        "description": "appended by the agent so the binding reads as scoped",
        "expression": "request.time < timestamp(\"2100-01-01T00:00:00Z\")",
        "title": "time-limited"
      },
      "members": [
        "user:alice@acme.example"
      ],
      "role": "roles/storage.objectViewer"
    },
    {
      "condition": {
        "description": "appended by the agent so the binding reads as scoped",
        "expression": "request.time < timestamp(\"2100-01-01T00:00:00Z\")",
        "title": "time-limited"
      },
      "members": [
        "user:bob@acme.example"
      ],
      "role": "roles/storage.admin"
    }
  ],
  "etag": "BwYCa15CondEvade=",
  "version": 3
}
